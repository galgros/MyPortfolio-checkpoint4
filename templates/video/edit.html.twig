{% extends 'base.html.twig' %}

{% block title %}Modifier Video{% endblock %}

{% block body %}
    <div class="content-info-top">
        <h1>Modifier Video</h1>
    </div>

    {{ include('video/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('video_index') }}"><button class="btn btn-info">Retour</button></a>

    {{ include('video/_delete_form.html.twig') }}
{% endblock %}
