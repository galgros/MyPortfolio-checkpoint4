{% extends 'base.html.twig' %}

{% block title %}Ajouter Music{% endblock %}

{% block body %}
    <div class="content-info-top">
        <h1>Ajouter une Music</h1>
    </div>

    {{ include('music/_form.html.twig') }}

    <a href="{{ path('music_index') }}"><button class="btn btn-info">Retour</button></a>
{% endblock %}
